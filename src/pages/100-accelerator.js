import * as React from 'react'
import styled from 'styled-components'
import WidthWrapper from 'components/WidthWrapper'
import Header from 'components/Header'

export default () => (
  <div>
    <h1>100+ Accelerator</h1>
  </div>
)
